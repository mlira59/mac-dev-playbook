---
- name: Jinja2 selectattr Test
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: selectattr Test
      debug:
        msg: "{{ [{'a': 1, 'b': 2}, {'a': 3}] | selectattr('a') | list }}"
